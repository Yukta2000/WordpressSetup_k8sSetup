---
# vars file for k8s_common
ansible_user: ec2-user
ansible_ssh_private_key_file: "{{ SSH_KEY_PATH }}"
ansible_become: true
repo: "https://packages.cloud.google.com/yum/repos/kubernetes-el7-\\$basearch"  
